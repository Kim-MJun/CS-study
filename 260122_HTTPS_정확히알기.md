# HTTPS 정확히 알기

> "HTTPS는 HTTP에 보안이 추가된 프로토콜입니다."

기술 면접에서 HTTPS에 대한 질문을 받으면 대부분 이렇게 답변합니다. 틀린 말은 아니지만, 면접관이 원하는 답은 아닙니다. 이 글에서는 면접관을 만족시킬 수 있는 HTTPS에 대한 깊이 있는 이해를 다룹니다.

---

## 1. HTTPS란 무엇인가

### 1.1 정의

**HTTPS (HyperText Transfer Protocol Secure)**는 HTTP 프로토콜에 TLS/SSL 암호화 계층을 추가한 프로토콜입니다.

```
HTTP  → 평문 통신 (Port 80)
HTTPS → 암호화 통신 (Port 443, HTTP + TLS/SSL)
```

### 1.2 HTTP와의 차이

| 구분        | HTTP   | HTTPS                 |
| ----------- | ------ | --------------------- |
| 포트        | 80     | 443                   |
| 프로토콜    | HTTP   | HTTP + TLS/SSL        |
| 데이터 전송 | 평문   | 암호화                |
| 인증서      | 불필요 | 필요 (SSL/TLS 인증서) |
| 보안        | 취약   | 강화                  |
| SEO         | 불리   | 유리                  |

### 1.3 용어 정리

- **SSL (Secure Sockets Layer)**: 넷스케이프가 개발한 초기 암호화 프로토콜 (현재는 사용 안 함)
- **TLS (Transport Layer Security)**: SSL의 후속 버전이자 현재 표준
- 실제로는 TLS를 사용하지만, 관습적으로 SSL이라고 부르는 경우가 많음

---

## 2. 왜 HTTPS가 필요한가

### 2.1 보안의 3요소

HTTPS는 다음 세 가지 보안 요소를 제공합니다:

#### 1) 기밀성 (Confidentiality) - 암호화

- 데이터를 암호화하여 제3자가 내용을 볼 수 없도록 보호
- 예: 로그인 정보, 신용카드 번호 등

#### 2) 무결성 (Integrity) - 변조 방지

- 데이터가 전송 중에 변조되지 않았음을 보장
- 중간자 공격(MITM) 방지

#### 3) 인증 (Authentication) - 신원 확인

- 서버가 신뢰할 수 있는 주체임을 인증
- 피싱 사이트 방지

### 2.2 실무적 필요성

- **법적 요구사항**: 개인정보 보호법, GDPR 등의 규제 준수
- **SEO 최적화**: Google은 HTTPS를 검색 순위 요소로 사용
- **브라우저 경고**: 최신 브라우저는 HTTP 사이트에 '안전하지 않음' 경고 표시
- **사용자 신뢰**: 자물쇠 아이콘으로 사용자의 신뢰도 향상

---

## 3. TLS Handshake 과정 (핵심)

TLS Handshake는 클라이언트와 서버가 안전한 연결을 수립하는 과정입니다. 이것이 HTTPS의 핵심 메커니즘입니다.

### 3.1 TLS 1.2 Handshake (2-RTT)

TLS 1.2는 2번의 왕복(Round Trip)이 필요합니다.

```
Client                                Server

1. ClientHello
   - TLS 버전
   - 지원하는 암호화 방식 목록
   - Client Random (난수)
   ────────────────────────────────>

2.                              ServerHello
                               - 선택된 TLS 버전
                               - 선택된 암호화 방식
                               - Server Random (난수)

                               Certificate
                               - SSL/TLS 인증서
                               - 서버의 공개키 포함

                               ServerHelloDone
   <────────────────────────────────

3. Certificate Verify
   - CA로 인증서 검증

   ClientKeyExchange
   - Pre-Master Secret 생성
   - 서버 공개키로 암호화하여 전송

   ChangeCipherSpec
   Finished
   ────────────────────────────────>

4.                              Master Secret 생성
                               - Client Random
                               - Server Random
                               - Pre-Master Secret

                               ChangeCipherSpec
                               Finished
   <────────────────────────────────

5. 암호화된 데이터 통신 시작
   <═══════════════════════════════>
```

**주요 단계 설명:**

1. **ClientHello**: 클라이언트가 지원하는 TLS 버전과 암호화 방식 목록 전송
2. **ServerHello**: 서버가 사용할 TLS 버전과 암호화 방식 선택, 인증서 전송
3. **인증서 검증**: 클라이언트가 CA를 통해 서버 인증서의 유효성 확인
4. **키 교환**: Pre-Master Secret을 서버의 공개키로 암호화하여 안전하게 전송
5. **세션 키 생성**: 양측이 동일한 Master Secret과 Session Key 생성
6. **암호화 통신 시작**: 대칭키(Session Key)를 사용한 빠른 데이터 통신

### 3.2 TLS 1.3 Handshake (1-RTT)

TLS 1.3은 1번의 왕복만 필요하여 더 빠릅니다.

```
Client                                Server

1. ClientHello
   - TLS 버전 (1.3)
   - 지원하는 암호화 방식
   - Client Random
   - KeyShare (DH 파라미터 포함)
   ────────────────────────────────>

2.                              ServerHello
                               - 선택된 암호화 방식
                               - Server Random
                               - KeyShare (DH 파라미터)

                               {EncryptedExtensions}
                               {Certificate}
                               {CertificateVerify}
                               {Finished}
   <────────────────────────────────

3. {Finished}
   ────────────────────────────────>

4. 암호화된 데이터 통신 시작
   <═══════════════════════════════>

   {} = 암호화된 메시지
```

**TLS 1.3의 개선점:**

- **1-RTT**: 왕복 횟수가 2번에서 1번으로 감소 (지연 시간 약 40% 감소)
- **0-RTT 재개**: 이전 세션을 재개할 때는 왕복 없이 즉시 데이터 전송 가능
- **강화된 보안**: 취약한 암호화 방식 제거 (RSA 키 교환, RC4, DES, 3DES 등)
- **항상 암호화**: ServerHello 이후 모든 메시지가 암호화됨
- **Forward Secrecy**: Diffie-Hellman 키 교환 방식만 사용

---

## 4. 암호화 방식의 이중 구조

HTTPS는 **비대칭 암호화**와 **대칭 암호화**를 함께 사용합니다. 왜 두 가지를 섞어 쓸까요?

### 4.1 비대칭 암호화 (Asymmetric Encryption)

**용도**: TLS Handshake 단계에서 안전한 키 교환

**원리:**

- 공개키(Public Key)와 개인키(Private Key) 쌍 사용
- 공개키로 암호화 → 개인키로만 복호화 가능
- 개인키로 서명 → 공개키로 검증 가능

**알고리즘:**

- RSA (Rivest-Shamir-Adleman)
- ECDHE (Elliptic Curve Diffie-Hellman Ephemeral)
- ECDSA (Elliptic Curve Digital Signature Algorithm)

**장점:**

- 안전한 키 교환 가능
- 인증 가능 (서버 신원 확인)

**단점:**

- 연산 속도가 매우 느림 (대칭 암호화 대비 약 100~1000배 느림)
- 암호화할 수 있는 데이터 크기 제한

### 4.2 대칭 암호화 (Symmetric Encryption)

**용도**: 실제 HTTP 데이터 전송 단계

**원리:**

- 하나의 세션키(Session Key)로 암호화와 복호화를 모두 수행
- 클라이언트와 서버가 동일한 키를 공유

**알고리즘:**

- AES-128, AES-256 (Advanced Encryption Standard)
- ChaCha20-Poly1305

**장점:**

- 연산 속도가 매우 빠름
- 대용량 데이터 암호화에 적합

**단점:**

- 키를 안전하게 공유하는 것이 어려움

### 4.3 하이브리드 방식

```
┌─────────────────────────────────────────────────────┐
│ TLS Handshake (비대칭 암호화)                        │
│ - 서버 인증                                          │
│ - 안전한 Session Key 교환                            │
│ - RSA, ECDHE 등 사용                                 │
│ - 느리지만 안전함                                    │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│ 실제 데이터 전송 (대칭 암호화)                       │
│ - HTTP 요청/응답 데이터                              │
│ - Session Key 사용                                   │
│ - AES, ChaCha20 등 사용                              │
│ - 빠르고 효율적                                      │
└─────────────────────────────────────────────────────┘
```

**왜 이렇게 사용할까?**

1. **보안**: 비대칭 암호화로 안전하게 키를 교환
2. **성능**: 대칭 암호화로 빠르게 대용량 데이터 전송
3. **효율성**: 각 방식의 장점만 활용

---

## 5. CA (Certificate Authority) - 인증 기관

### 5.1 CA의 역할

CA는 디지털 인증서를 발급하고 관리하는 신뢰할 수 있는 제3자 기관입니다.

**주요 역할:**

- SSL/TLS 인증서 발급
- 인증서 소유자의 신원 확인
- 인증서의 유효성 보증

**대표적인 CA:**

- Let's Encrypt (무료)
- DigiCert
- GlobalSign
- Comodo

### 5.2 인증서 검증 과정

```
1. 브라우저가 서버에 연결
   ↓
2. 서버가 SSL 인증서 전송
   ↓
3. 브라우저가 인증서 확인
   - 인증서 유효기간 확인
   - 도메인 일치 확인
   - CA 서명 검증
   ↓
4. CA의 공개키로 서명 검증
   - 브라우저에 내장된 Root CA 목록 사용
   - 인증서 체인 검증
   ↓
5. 검증 성공 → 안전한 연결 수립
   검증 실패 → 경고 표시
```

### 5.3 인증서 체인 (Certificate Chain)

```
Root CA (루트 인증 기관)
    ↓ 서명
Intermediate CA (중간 인증 기관)
    ↓ 서명
End-Entity Certificate (최종 서버 인증서)
```

**왜 체인 구조를 사용할까?**

- Root CA의 개인키 보호 (직접 서명하지 않음)
- 분산된 인증서 발급 관리
- Root CA 손상 시 피해 최소화

### 5.4 인증서에 포함된 정보

- **Subject**: 인증서 소유자 정보 (도메인명, 회사명 등)
- **Issuer**: 인증서를 발급한 CA 정보
- **Valid From / Valid To**: 유효 기간
- **Public Key**: 서버의 공개키
- **Signature Algorithm**: 서명에 사용된 알고리즘
- **Signature**: CA의 디지털 서명

---

## 6. 실무에서의 HTTPS

### 6.1 인증서 발급 및 관리

**1) 인증서 종류**

| 종류                         | 검증 수준            | 발급 시간 | 용도             | 비용      |
| ---------------------------- | -------------------- | --------- | ---------------- | --------- |
| DV (Domain Validation)       | 도메인 소유권만 확인 | 몇 분     | 일반 웹사이트    | 무료~저렴 |
| OV (Organization Validation) | 조직 실재 확인       | 며칠      | 기업 사이트      | 중간      |
| EV (Extended Validation)     | 엄격한 신원 확인     | 1~2주     | 금융, 전자상거래 | 비쌈      |

**2) Let's Encrypt (무료 자동 인증서)**

```bash
# Certbot 설치 (Ubuntu 예시)
sudo apt-get install certbot python3-certbot-nginx

# 인증서 발급
sudo certbot --nginx -d example.com -d www.example.com

# 자동 갱신 설정
sudo certbot renew --dry-run
```

**3) 인증서 유효기간 단축 추세**

- 2025년 현재: 최대 398일 (약 13개월)
- 2029년까지: 47일로 단축 예정 (CA/Browser Forum 결정)
- 이유: 보안 강화, 자동화 촉진

### 6.2 웹 서버 설정

**Nginx 예시:**

```nginx
server {
    listen 443 ssl http2;
    server_name example.com;

    # TLS 인증서
    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;

    # TLS 버전 설정
    ssl_protocols TLSv1.2 TLSv1.3;

    # TLS 1.3만 사용하는 경우
    # ssl_protocols TLSv1.3;

    # 암호화 스위트 (TLS 1.2용)
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305';
    ssl_prefer_server_ciphers off;

    # OCSP Stapling (인증서 상태 확인 최적화)
    ssl_stapling on;
    ssl_stapling_verify on;

    # HSTS (Strict-Transport-Security)
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 세션 캐시 설정 (성능 최적화)
    ssl_session_cache shared:SSL:50m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    location / {
        proxy_pass http://backend;
    }
}

# HTTP → HTTPS 리다이렉트
server {
    listen 80;
    server_name example.com;
    return 301 https://$server_name$request_uri;
}
```

### 6.3 성능 최적화

**1) Session Resumption (세션 재사용)**

- 이전 연결의 세션을 재사용하여 Handshake 생략
- TLS 1.2: Session ID, Session Ticket
- TLS 1.3: PSK (Pre-Shared Key)

**2) OCSP Stapling**

- 서버가 CA로부터 인증서 상태를 미리 받아옴
- 클라이언트가 CA에 직접 조회할 필요 없음
- Handshake 시간 단축

**3) TLS Termination 분산**

```
클라이언트 → [CDN/Load Balancer] → 내부 서버
              TLS 여기서 종료      HTTP로 통신
```

- CDN 또는 로드밸런서에서 TLS 처리
- 내부는 HTTP로 통신 (안전한 내부망)
- CPU 부하 분산

**4) HTTP/2, HTTP/3 활용**

- HTTP/2: 멀티플렉싱으로 연결 재사용
- HTTP/3: QUIC (TLS 1.3 기반) 사용, 0-RTT 지원

### 6.4 보안 강화

**1) HSTS (HTTP Strict Transport Security)**

```http
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
```

- 브라우저가 항상 HTTPS로만 접속하도록 강제
- MITM 공격 방지

**2) Certificate Pinning**

- 특정 인증서 또는 CA만 신뢰하도록 설정
- 중간자 공격 방어 강화
- 모바일 앱에서 주로 사용

**3) 정기적인 인증서 갱신**

```bash
# Certbot 자동 갱신 (Cron)
0 3 * * * certbot renew --quiet --post-hook "systemctl reload nginx"
```

---

## 7. 일반적인 문제 및 해결

### 7.1 TLS Handshake 실패

**주요 원인:**

1. **인증서 만료**: 정기적인 갱신 필요
2. **잘못된 인증서 체인**: 중간 인증서 누락
3. **프로토콜 버전 불일치**: 구형 클라이언트 vs 최신 서버
4. **암호화 스위트 불일치**: 공통 암호화 방식 없음
5. **시스템 시간 오류**: 인증서 유효기간 검증 실패

**디버깅 방법:**

```bash
# OpenSSL로 연결 테스트
openssl s_client -connect example.com:443 -tls1_2
openssl s_client -connect example.com:443 -tls1_3

# 인증서 정보 확인
openssl x509 -in certificate.crt -text -noout

# 인증서 체인 확인
openssl s_client -connect example.com:443 -showcerts
```

### 7.2 Mixed Content 경고

**문제:**

- HTTPS 페이지에서 HTTP 리소스 로드 시 브라우저 경고

**해결:**

```html
<!-- 나쁨 -->
<script src="http://example.com/script.js"></script>
<img src="http://example.com/image.jpg" />

<!-- 좋음 -->
<script src="https://example.com/script.js"></script>
<img src="https://example.com/image.jpg" />

<!-- 프로토콜 상대 URL (권장하지 않음) -->
<script src="//example.com/script.js"></script>
```

### 7.3 성능 이슈

**증상:**

- HTTPS 전환 후 응답 속도 저하

**해결책:**

1. TLS 1.3 사용
2. HTTP/2 활성화
3. CDN 도입
4. Session Resumption 활성화
5. OCSP Stapling 설정
6. 하드웨어 가속 활용 (AES-NI)

---

## 9. 면접 예상 질문 & 답변

### Q1. HTTPS는 무엇인가요?

**좋지 않은 답변:**

> "HTTP에 보안이 추가된 프로토콜입니다."

**좋은 답변:**

> "HTTPS는 HTTP 프로토콜에 TLS/SSL 암호화 계층을 추가한 프로토콜로, 포트 443을 사용합니다. 데이터의 기밀성, 무결성, 인증을 제공하여 안전한 통신을 보장합니다.
>
> 구체적으로는 TLS Handshake 과정을 통해 서버를 인증하고, 비대칭 암호화로 세션 키를 안전하게 교환한 뒤, 대칭 암호화로 실제 데이터를 빠르게 주고받습니다."

---

### Q2. TLS Handshake 과정을 설명해주세요.

**답변:**

> "TLS 1.2 기준으로 설명드리겠습니다. 크게 4단계로 나뉩니다.
>
> 1단계: 클라이언트가 ClientHello 메시지로 지원하는 TLS 버전과 암호화 방식 목록을 서버에 전송합니다.
>
> 2단계: 서버가 ServerHello로 사용할 TLS 버전과 암호화 방식을 선택하고, SSL 인증서를 전송합니다.
>
> 3단계: 클라이언트가 CA를 통해 인증서를 검증한 후, Pre-Master Secret을 생성해 서버의 공개키로 암호화하여 전송합니다.
>
> 4단계: 양측이 Client Random, Server Random, Pre-Master Secret으로 동일한 Master Secret과 Session Key를 생성하고, 이후 대칭키 암호화로 데이터를 주고받습니다.
>
> TLS 1.3에서는 이 과정이 1-RTT로 단축되어 더 빠릅니다."

---

### Q3. 왜 HTTPS는 비대칭 암호화와 대칭 암호화를 함께 사용하나요?

**답변:**

> "각 암호화 방식의 장점만 활용하기 위해서입니다.
>
> 비대칭 암호화(RSA, ECDHE)는 안전한 키 교환과 서버 인증이 가능하지만, 연산 속도가 매우 느립니다. 반면 대칭 암호화(AES)는 빠르지만, 키를 안전하게 공유하는 것이 어렵습니다.
>
> 따라서 TLS Handshake 단계에서는 비대칭 암호화로 안전하게 세션 키를 교환하고, 실제 데이터 전송 시에는 대칭 암호화로 빠르게 통신합니다. 이렇게 하이브리드 방식을 사용하면 보안과 성능을 모두 만족할 수 있습니다."

---

### Q4. CA(Certificate Authority)의 역할은 무엇인가요?

**답변:**

> "CA는 SSL/TLS 인증서를 발급하고 관리하는 신뢰할 수 있는 제3자 기관입니다.
>
> 주요 역할은 세 가지입니다. 첫째, 서버의 신원을 확인하고 인증서를 발급합니다. 둘째, 자신의 개인키로 인증서에 디지털 서명을 하여 위변조를 방지합니다. 셋째, 인증서의 유효성을 보증합니다.
>
> 클라이언트는 브라우저에 내장된 Root CA 목록을 통해 서버 인증서의 서명을 검증함으로써, 해당 서버가 신뢰할 수 있는지 판단합니다. 대표적인 CA로는 Let's Encrypt, DigiCert, GlobalSign 등이 있습니다."

---

### Q5. TLS 1.2와 1.3의 차이점은 무엇인가요?

**답변:**

> "크게 세 가지 측면에서 차이가 있습니다.
>
> 첫째, 성능입니다. TLS 1.3은 Handshake를 1-RTT로 단축하여 지연 시간을 약 40% 줄였고, 0-RTT Resumption으로 재연결 시 왕복 없이 즉시 데이터 전송이 가능합니다.
>
> 둘째, 보안입니다. TLS 1.3은 RSA 키 교환, RC4, DES, 3DES 같은 취약한 알고리즘을 모두 제거했고, Forward Secrecy를 보장하는 ECDHE, DHE만 지원합니다. 또한 ServerHello 이후 모든 메시지를 암호화합니다.
>
> 셋째, 단순성입니다. 암호화 스위트 옵션을 15~20개에서 5개로 줄여 설정 오류를 방지했습니다.
>
> 다만 TLS 1.3은 구형 클라이언트(IE, 구형 안드로이드/iOS)를 지원하지 않아, 레거시 환경에서는 TLS 1.2와 병행 운영이 필요합니다."

---

### Q6. HTTPS를 사용하면 성능이 떨어지지 않나요?

**답변:**

> "초기 연결 시 TLS Handshake로 인해 약간의 오버헤드가 있지만, 최근에는 오히려 HTTPS가 더 빠른 경우도 많습니다.
>
> 이유는 세 가지입니다. 첫째, HTTP/2는 HTTPS에서만 동작하는데, 멀티플렉싱과 헤더 압축으로 HTTP/1.1보다 훨씬 빠릅니다. 둘째, TLS 1.3의 1-RTT Handshake와 0-RTT Resumption으로 지연 시간이 크게 줄었습니다. 셋째, Session Resumption과 OCSP Stapling 같은 최적화 기법들이 있습니다.
>
> 또한 CDN을 활용하면 TLS Termination을 엣지에서 처리하고, 하드웨어 가속(AES-NI)을 사용하면 CPU 오버헤드도 최소화할 수 있습니다. 실제로 Google 같은 대형 서비스들은 HTTPS 전환 후 성능 향상을 보고했습니다."

---

### Q7. Forward Secrecy(전방향 안전성)란 무엇인가요?

**답변:**

> "Forward Secrecy는 서버의 개인키가 유출되더라도 과거의 통신 내용을 복호화할 수 없도록 보장하는 특성입니다.
>
> TLS 1.2의 RSA 키 교환 방식은 서버의 개인키로 모든 세션을 복호화할 수 있어 위험했습니다. 반면 ECDHE나 DHE 같은 Ephemeral(임시) 키 교환 방식은 세션마다 새로운 임시 키 쌍을 생성하고, 사용 후 즉시 폐기합니다.
>
> TLS 1.3은 Forward Secrecy를 강제하기 위해 RSA 키 교환을 완전히 제거하고, ECDHE와 DHE만 지원합니다. 이로써 과거 통신의 안전성이 크게 향상되었습니다."

---

### Q8. Mixed Content 경고는 무엇이고, 어떻게 해결하나요?

**답변:**

> "Mixed Content는 HTTPS 페이지에서 HTTP 리소스(이미지, 스크립트, CSS 등)를 로드할 때 발생하는 보안 경고입니다. 브라우저는 이를 차단하거나 경고를 표시합니다.
>
> 해결 방법은 세 가지입니다. 첫째, 모든 리소스를 HTTPS로 변경합니다. 둘째, Content-Security-Policy 헤더로 HTTP 리소스를 차단합니다. 셋째, 외부 리소스는 HTTPS를 지원하는 CDN으로 전환합니다.
>
> 프로토콜 상대 URL(//example.com/image.jpg)은 과거에 사용되었지만, 현재는 명시적으로 HTTPS를 사용하는 것이 권장됩니다."

---

## 10. 요약 및 핵심 정리

### 핵심 키워드

- **HTTPS = HTTP + TLS/SSL**
- **포트 443**
- **기밀성, 무결성, 인증**
- **TLS Handshake**: 안전한 연결 수립 과정
- **하이브리드 암호화**: 비대칭(키 교환) + 대칭(데이터 전송)
- **CA**: 신뢰할 수 있는 제3자 인증 기관
- **TLS 1.3**: 더 빠르고 안전한 최신 버전

### 면접에서 강조할 포인트

1. **HTTPS의 3요소**: 암호화, 변조 방지, 인증
2. **TLS Handshake 과정**: ClientHello → ServerHello → 인증서 검증 → 키 교환 → 세션 키 생성
3. **하이브리드 암호화**: 비대칭으로 안전하게 키 교환, 대칭으로 빠르게 데이터 전송
4. **CA의 역할**: 인증서 발급, 서명, 검증
5. **TLS 1.3 개선점**: 1-RTT, 보안 강화, Forward Secrecy

### 추가 학습 자료

- [RFC 8446 - TLS 1.3](https://datatracker.ietf.org/doc/html/rfc8446)
- [RFC 5246 - TLS 1.2](https://datatracker.ietf.org/doc/html/rfc5246)
- [Cloudflare Learning: TLS](https://www.cloudflare.com/learning/ssl/what-is-ssl/)
- [Let's Encrypt](https://letsencrypt.org/)
- [SSL Labs Server Test](https://www.ssllabs.com/ssltest/)

---

> 💡 **팁**: 기술 면접에서는 단순히 정의를 암기하는 것보다, "왜 이렇게 설계되었는지", "어떤 문제를 해결하기 위한 것인지"를 이해하고 설명할 수 있어야 합니다. HTTPS는 보안과 성능의 균형을 맞추기 위한 다양한 설계 결정들이 담긴 프로토콜입니다.

---

**작성일**: 2026-01-22  
**작성자**: 5년차 프론트엔드 개발자  
**키워드**: #HTTPS #TLS #SSL #암호화 #보안 #웹개발 #네트워크
